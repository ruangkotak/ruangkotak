# Contributing

- Branch off `main` for any change (`git checkout -b your-branch-name`).
- Open a PR into `main` when ready — don't push directly, branch protection blocks it.
- Keep `.env`, real API keys, and audit data out of commits (`.env.example` shows the shape).
- One focused change per PR — easier to review, easier to revert if something's wrong.
