# PORT=8123

# Pre-launch gate. Set BOTH on the host once deployed — every request needs
# HTTP Basic Auth with this username/password. Leave both unset for local dev.
# GATE_USER=ruangkotak
# GATE_PASS=change-me
